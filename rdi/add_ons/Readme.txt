only need to include the rpro_ files if rpro is being used.
or just keep it and its flexibly set by the config


libraries must have the name of the folder prefixed to all of the files in the folder
use the same name for the classes between libs so they can be called very abstract